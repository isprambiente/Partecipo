<%= form_with(model: admin_group, local: true) do |form| %>
  <% if admin_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_group.errors.count, "error") %> prohibited this admin_group from being saved:</h2>

      <ul>
        <% admin_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :user_ids %>
    <%= form.text_field :user_ids %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
